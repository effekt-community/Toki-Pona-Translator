module main

import src/lib
import src/trees
import src/additionalLists
import src/translate
import src/buildTree



def main(): Unit = {
  val l1: List[String] = ["mi", "moku", "e", "moku"]
  val l2: List[String] = ["mama", "ona", "li", "sona", "mute", "e", "toki", "pona"]
  printTree(splitSentence(l1))
  printTree(traverseTree(splitSentence(l1)))
}